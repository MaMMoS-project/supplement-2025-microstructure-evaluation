[workspace]
authors = []
channels = ["conda-forge"]
name = "supplement-2025-microstructure-evaluation"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
full = "python hard-magnet-grains.py"

[dependencies]
esys-escript = "==5.10"
mammos = "==0.13.0"
ruff = ">=0.12.10,<0.13"

[feature.cuda.pypi-dependencies]
jax = { version = "==0.7.1", extras = ["cuda12"] }

[environments]
cuda = ["cuda"]
